<Window x:Class="Primzahlengenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Primzahlengenerator"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <Storyboard x:Key="loadingRotation">
            <DoubleAnimation Storyboard.TargetName="image1" Storyboard.TargetProperty="(Image.RenderTransform).(RotateTransform.Angle)" From="0" To="360" Duration="0:0:2.0" RepeatBehavior="Forever" />
        </Storyboard>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Orientation="Vertical" Grid.Column="0" Margin="5">
            <Label Content="Threads:"/>
            <TextBox Height="30" Width="200" 
                     HorizontalAlignment="Left" VerticalContentAlignment="Center"
                     x:Name="ThreadsBox" Margin="5"
                     />

            <Label Content="Primzahl:"/>
            <TextBox Height="30" Width="200" 
                     HorizontalAlignment="Left" VerticalContentAlignment="Center"
                     x:Name="PrimBox" Margin="5"
                     />

            <Button Content="Berechnen"
                    Height="30" Width="200"
                    HorizontalAlignment="Left"
                    Margin="5 15"
                    Click="Button_Click"
                    />

            <Label Content="Primzahl:" Margin="0 25 0 0"/>
            <TextBox Height="30" Width="200" 
                     HorizontalAlignment="Left" VerticalContentAlignment="Center"
                     x:Name="Zahlbox" Margin="5" IsEnabled="False"
                     />
        </StackPanel>

        <Grid Grid.Column="1">
            <Image Name="image1" Stretch="Fill" Width="124" Height="124" 
                   Source="pack://application:,,,/loading_big.png"
                   Visibility="Hidden"
                   >
                <Image.RenderTransform>
                    <RotateTransform Angle="0" CenterX="62" CenterY="62" />
                </Image.RenderTransform>
            </Image>

        </Grid>
    </Grid>
</Window>
