<Window x:Class="KMeans.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KMeans"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1000">
    <DockPanel>
        <!-- MENU ITEMS -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="XML _laden" Click="Laden_Click"/>
            <MenuItem Header="XML _speichern" Click="Speichern_Click"/>
            <MenuItem Header="Canvas leeren" Click="Leeren_Click"/>
        </Menu>

        <!-- STATUS BAR -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem Content="{Binding aktuellerStatus}" HorizontalContentAlignment="Center"/>
        </StatusBar>

        <!-- GLOBAL GRID -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT UPPER GRID -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- PERSON -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="2.5*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Vertical" Grid.Column="0" Margin="5">
                        <Label Margin="2" Height="30" Content="Name"/>
                        <Label Margin="2" Height="30" Content="PixelX"/>
                        <Label Margin="2" Height="30"  Content="PixelY"/>
                        <Button Margin="2" Height="30" Content="Person erstellen" Click="Person_Click"/>
                    </StackPanel>

                    <StackPanel Orientation="Vertical" Grid.Column="1" Margin="5">
                        <TextBox Margin="2" x:Name="NameBox" Height="30"/>
                        <TextBox Margin="2" x:Name="XBox" Height="30"/>
                        <TextBox Margin="2" x:Name="YBox" Height="30"/>
                    </StackPanel>
                </Grid>

                <!-- LEFT LOWER GRID -->
                <Grid Grid.Row="1">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Label Margin="2" Content="Anzahl Cluster:" Height="30" Width="100"/>
                            <TextBox Margin="2" x:Name="KBox" Height="30" Width="100"/>
                            <Button Margin="25 2" Click="Cluster_Click" Content="Cluster erzeugen" Height="30" Width="100" x:Name="ErzeugenBtn"/>
                        </StackPanel>

                        <Button Margin="2 50" x:Name="WeiterBtn"
                                Content="Weiter >" 
                                Height="30" Width="100" 
                                HorizontalAlignment="Center"
                                IsEnabled="False"
                                Click="Weiter_Click"
                                />
                    </StackPanel>

                </Grid>
            </Grid>

            <!-- RIGHT GRID -->
            <Grid Grid.Column="1" Width="400" Height="400">
                <Canvas Height="400" Width="400" x:Name="MyCanvas" ClipToBounds="True"/>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
